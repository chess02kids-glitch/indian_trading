-r base.txt

# Research, VectorBT, and experiment tracking.
# mlflow is the `tracking` extra in pyproject; see the dependency note
# there (cryptography/pyarrow pin deadlock with every mlflow release).
mlflow>=2.15,<4.0
numpy>=1.26,<3.0
# vectorbt 0.26 is incompatible with plotly 6.x (Heatmapgl removed).
plotly>=5.0,<6.0
vectorbt>=0.26,<1.0
